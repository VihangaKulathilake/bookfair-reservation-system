import React from 'react';
import { Typography, Box } from '@mui/material';

const ReservationForm = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                New Reservation
            </Typography>
            <Typography>Reservation form will go here.</Typography>
        </Box>
    );
};

export default ReservationForm;
